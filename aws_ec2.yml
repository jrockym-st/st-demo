# Dynamic inventory that queries the EC2 inventory
plugin: aws_ec2
#boto_pr=ofile: default
regions:
  - us-east-1
#filters:
#  tag:name:
#    - worker
strict_permissions: False
keyed_groups:
  - key: tags['name']
    prefix: aws
hostnames:
  - private-ip-address
compose:
  ansible_host: private_ip_address
